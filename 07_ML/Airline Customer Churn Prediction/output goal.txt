Create a complete Jupyter Notebook (.ipynb) for a Machine Learning project using a dataset named "fact_activity_cleaned".

The notebook must be professional, well-structured, and suitable for a data science portfolio, with clear markdown explanations.

========================
📊 DATASET CONTEXT
========================
The dataset contains customer loyalty activity over time (monthly level), with columns such as:
- loyalty_number
- year
- month
- total_flights
- distance
- points_accumulated
- points_redeemed
- dollar_cost_points_redeemed
- customer_id (or similar)

⚠️ IMPORTANT:
The dataset may contain additional columns not listed above.
You MUST:
- Automatically detect and use all relevant columns
- Not restrict modeling to only the listed columns
- Handle time-series behavior properly

========================
🎯 OBJECTIVE
========================

⚠️ Churn Prediction (MAIN TASK)

Goal:
Predict whether a customer will stop flying.

Definition of churn:
- A customer is considered churned if they have **no flights for the next 6 consecutive months**

Target:
- Create a binary label:
  - 1 = churn
  - 0 = active

========================
🧠 FEATURE ENGINEERING (CRITICAL)
========================

You MUST create time-based and behavioral features such as:

1. Recency Features:
- months_since_last_flight

2. Frequency Features:
- avg_flights_last_3_months
- avg_flights_last_6_months

3. Trend Features:
- flight_trend (increase or decrease over time)
- points_trend

4. Engagement Features:
- total_points_accumulated
- redemption_ratio = points_redeemed / (points_accumulated + 1)

5. Activity Drop Indicators:
- drop_in_flights (compare recent vs past periods)
- drop_in_points

6. Aggregations:
- customer-level aggregates (group by loyalty_number)

========================
⚙️ TECHNICAL REQUIREMENTS
========================

- Use Python
- Libraries:
  - pandas, numpy
  - matplotlib, seaborn
  - scikit-learn
  - xgboost or lightgbm

========================
🧱 PIPELINE STRUCTURE
========================

1. Data Loading
- Load CSV: "fact_activity_cleaned.csv"

2. Data Preparation
- Sort data by customer and time (year, month)
- Create proper time index
- Group data at customer level

3. Target Creation (IMPORTANT)
- For each customer and time point:
  - Check if total_flights = 0 for next 6 months
  - Assign churn label

4. Exploratory Data Analysis (EDA)
- Activity trends over time
- Distribution of flights, distance, points
- Churn vs non-churn comparison

5. Feature Engineering
- Build recency, frequency, trend, and engagement features
- Handle missing values
- Encode categorical variables if present

6. Feature Selection
- Use correlation or model-based importance
- Keep most relevant features

7. Model Training
- Train:
  - Logistic Regression
  - Random Forest
  - XGBoost
- Use proper train/test split (time-based split preferred)

8. Evaluation
- Accuracy
- Precision, Recall, F1-score
- ROC-AUC
- Confusion Matrix

9. Model Interpretation
- Feature importance
- Identify key drivers of churn

========================
📈 BUSINESS INSIGHTS
========================

Provide clear insights such as:
- What behavior leads to churn?
- How early can churn be detected?
- Which customers are high risk?

========================
🎯 OUTPUT
========================

- Fully executable notebook
- Clean and professional structure
- Proper handling of time-series data
- Strong feature engineering
- Clear business-oriented conclusions

Ensure the notebook dynamically adapts to the dataset and does not rely on hardcoded column assumptions.